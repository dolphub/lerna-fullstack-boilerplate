{
  "name": "@dolphub/common",
  "private": true,
  "version": "0.1.0",
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "license": "MIT",
  "scripts": {
    "lint": "tslint -p tsconfig.json -c tslint.json",
    "start:dev": "tsc -p tsconfig.json -w --preserveWatchOutput",
    "start": "tsc -p tsconfig.json --watch",
    "clean-lib": "rimraf lib",
    "prebuild": "yarn clean-lib",
    "build": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "lodash": "^4.17.11",
    "tslint": "^5.18.0",
    "typeorm": "^0.2.18",
    "typescript": "^3.5.3"
  },
  "devDependencies": {
    "rimraf": "^3.0.0"
  }
}
